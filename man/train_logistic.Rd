% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train_logistic.R
\name{train_logistic}
\alias{train_logistic}
\title{Train a logistic regression model}
\usage{
train_logistic(X, y, lambda = 0, penalty = "none", tol = 1e-06, max_iter = 100)
}
\arguments{
\item{X}{A numeric matrix where each row is an observation and each column is a feature.}

\item{y}{A numeric vector of binary responses (0, 1) corresponding to each row of \code{X}.}

\item{lambda}{Regularization parameter for L2 penalty (default is 0).}

\item{penalty}{A character string specifying the type of regularization. Default is \code{"none"}.}

\item{tol}{A numeric value specifying the tolerance for convergence (default is \code{1e-6}).}

\item{max_iter}{Maximum number of iterations for gradient descent (default is \code{100}).}
}
\value{
A list containing the trained model, including:
\describe{
  \item{coefficients}{The estimated coefficients of the logistic regression model.}
  \item{converged}{A logical indicating whether the optimization converged.}
  \item{iterations}{The number of iterations performed.}
}
}
\description{
This function trains a logistic regression model using gradient descent.
It supports optional L2 regularization.
}
\examples{
X <- matrix(c(1, 2, 3, 4, 5, 6), ncol = 2)
y <- c(0, 1, 0)
model <- train_logistic(X, y)
print(model$coefficients)

}
